revolte:
  version: "1.0"
  project: eak-deptest
  appName: webservice-test 
  env:
    qa:
      branch: test-qa
      services:
        rid_1:
          type: WEB_SERVICE
          name: web_service
          entryPoint: app.js
          properties:
            health: /user-vault/health
            loadBalancerMode: 'shared'
            port: 8080
            variables:
              BASE_URL: /api/v1
              INFO: "#{info}"
          specifications:
            cpu: 2
            memory: 2
            storage: 30
